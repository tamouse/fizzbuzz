#!/usr/bin/env ruby
# -*- ruby -*-
lib = File.expand_path("../../lib", __FILE__)
$:.unshift(lib) unless $:.include?(lib)
require 'rubygems'
require 'bundler'
Bundler.setup(:default)
require 'fizzbuzz'

FizzBuzz.start
